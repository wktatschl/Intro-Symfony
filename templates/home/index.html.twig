{% extends 'base.html.twig' %}

{% block title %}Home
{% endblock %}

{% block body %}
{% if app.user %}
<h1>Bonjour {{ app.user.email }}</h1>
{% else %}
<h1>Bienvenue sur la page d'accueil</h1>
{% endif %}
        {% for categ in categs %}
			<div>{{ categ.title }} <br>
            <button class="btn btn-primary">
                <a class='navLink'href="{{ path('details', { id:categ.id }) }}">Voir plus</a>
            </button>
            </div>
		{% endfor %}

{% endblock %}
